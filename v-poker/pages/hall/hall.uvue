<template>
	<view class="vp-page hall-page">
		<!-- 背景图 -->
		<image class="hall-bg" src="/static/hall-bg.png" mode="aspectFill" />
		<view class="hall-bg-mask" />

		<view class="hall-container">
			<!-- 左侧边栏 -->
			<view class="hall-sidebar">
				<view class="hall-logo">
					<view class="hall-logo-badge">
						<text class="hall-logo-spade">♠</text>
					</view>
					<text class="hall-logo-text">V-POKER</text>
					<text class="hall-logo-sub">金樽俱乐部</text>
				</view>

				<view class="hall-nav">
					<view class="hall-nav-item hall-nav-active">
						<text class="hall-nav-icon">🎮</text>
						<text class="hall-nav-text hall-nav-text-active">大厅</text>
					</view>
					<view class="hall-nav-item" hover-class="hall-pressed" @click="goMyRooms">
						<text class="hall-nav-icon">📋</text>
						<text class="hall-nav-text">我的房间</text>
					</view>
					<view class="hall-nav-item" hover-class="hall-pressed" @click="goRoomList">
						<text class="hall-nav-icon">🔍</text>
						<text class="hall-nav-text">房间列表</text>
					</view>
					<view class="hall-nav-item" hover-class="hall-pressed" @click="goWallet">
						<text class="hall-nav-icon">💰</text>
						<text class="hall-nav-text">钱包</text>
					</view>
				</view>

				<view class="hall-user" hover-class="hall-pressed" @click="goProfile">
					<vp-avatar :name="displayName" :size="40" />
					<view class="hall-user-meta">
						<text class="hall-user-name">{{ displayName }}</text>
						<view class="hall-user-coin">
							<text class="hall-user-coin-text">{{ balanceText }}</text>
						</view>
					</view>
				</view>
			</view>

			<!-- 右侧主内容 -->
			<view class="hall-main">
				<!-- 顶部标题 -->
				<view class="hall-header">
					<text class="hall-title">选择玩法</text>
					<text class="hall-subtitle">13款游戏 · 私密对局 · 安全结算</text>
				</view>

				<!-- 游戏卡片网格 -->
				<view class="hall-games">
					<view v-for="g in games" :key="g.game_type" class="hall-game"
						hover-class="hall-game-pressed" @click="enterGame(g)">
						<image class="hall-game-bg" :src="gameBg(g.game_type)" mode="aspectFill" />
						<view class="hall-game-mask" />
						<view class="hall-game-content">
							<text class="hall-game-name">{{ g.name }}</text>
							<view class="hall-game-meta">
								<text class="hall-game-meta-text">底分 {{ baseOf(g.game_type) }}</text>
								<text class="hall-game-dot">·</text>
								<text class="hall-game-meta-text">2-9人</text>
							</view>
							<view class="hall-game-footer">
								<view class="hall-game-online">
									<view class="hall-online-dot" />
									<text class="hall-online-text">{{ onlineCount(g.game_type) }}人在线</text>
								</view>
								<view class="hall-game-btn">
									<text class="hall-game-btn-text">快速入局</text>
								</view>
							</view>
						</view>
					</view>
				</view>
			</view>
		</view>
	</view>
</template>

<script setup lang="uts">
	import { ref, computed } from 'vue'
	import { useAuthStore } from '@/store/auth.uts'
	import { useWalletStore } from '@/store/wallet.uts'
	import { GAME_ENTRIES, GameEntry } from '@/utils/games.uts'
	import { formatAmount } from '@/utils/format.uts'

	const auth = useAuthStore()
	const wallet = useWalletStore()

	const games = GAME_ENTRIES

	const displayName = computed((): string => {
		return auth.userId || '玩家'
	})

	const balanceText = computed((): string => {
		return formatAmount(wallet.balance)
	})

	onShow(() => {
		auth.restore()
		wallet.refresh()
	})

	const gameBg = (gt: string): string => {
		const map: Record<string, string> = {
			texas_holdem: '/static/table/texas-holdem.png',
			zha_jin_hua: '/static/table/zha-jin-hua.png',
			niu_niu: '/static/table/niu-niu.png',
			san_gong: '/static/table/san-gong.png',
			squid_game: '/static/table/squid-game.png',
			guandan: '/static/table/guandan.png',
			fight_bomb: '/static/table/fight_bomb.png',
			omaha: '/static/table/omaha.png',
			thirteen_water: '/static/table/thirteen_water.png',
			double_kong: '/static/table/double_kong.png',
			hong_wu: '/static/table/hong_wu.png',
			pineapple: '/static/table/pineapple.png',
			short_deck: '/static/table/short_deck.png',
		}
		return map[gt] || '/static/table/texas-holdem.png'
	}

	const baseOf = (gt: string): string => {
		if (gt == 'zha_jin_hua') return '50'
		if (gt == 'squid_game') return '200'
		return '100'
	}

	const onlineCount = (gt: string): number => {
		const seed = gt.charCodeAt(0) + gt.charCodeAt(gt.length - 1)
		return 50 + (seed % 200)
	}

	const enterGame = (g: GameEntry) => {
		uni.navigateTo({ url: `/pages/room-list/room-list?game_type=${g.game_type}&name=${g.name}` })
	}

	const goMyRooms = () => {
		uni.navigateTo({ url: '/pages/my-rooms/my-rooms' })
	}

	const goRoomList = () => {
		uni.navigateTo({ url: '/pages/room-list/room-list' })
	}

	const goWallet = () => {
		uni.navigateTo({ url: '/pages/wallet/wallet' })
	}

	const goProfile = () => {
		uni.navigateTo({ url: '/pages/profile/profile' })
	}
</script>

<style lang="scss">
	.hall-page {
		flex: 1;
		background-color: #0a0e14;
	}

	.hall-bg {
		position: absolute;
		width: 100%;
		height: 100%;
	}

	.hall-bg-mask {
		position: absolute;
		width: 100%;
		height: 100%;
		background: linear-gradient(
			135deg,
			rgba(10, 14, 20, 0.9) 0%,
			rgba(10, 14, 20, 0.7) 100%
		);
	}

	/* 横屏容器 */
	.hall-container {
		flex: 1;
		flex-direction: row;
	}

	/* 左侧边栏 */
	.hall-sidebar {
		width: 240px;
		background: rgba(15, 18, 25, 0.9);
		border-right-width: 1px;
		border-right-style: solid;
		border-right-color: rgba(212, 175, 55, 0.15);
		flex-direction: column;
		padding: 24px 16px;
	}

	.hall-logo {
		align-items: center;
		margin-bottom: 40px;
	}

	.hall-logo-badge {
		width: 56px;
		height: 64px;
		background: linear-gradient(180deg, #1c232e 0%, #0a0e14 100%);
		border-width: 2px;
		border-style: solid;
		border-color: #d4af37;
		border-radius: 8px 8px 28px 28px;
		align-items: center;
		justify-content: center;
		margin-bottom: 12px;
	}

	.hall-logo-spade {
		color: #d4af37;
		font-size: 28px;
		font-weight: bold;
	}

	.hall-logo-text {
		color: #d4af37;
		font-size: 20px;
		font-weight: bold;
		letter-spacing: 3px;
	}

	.hall-logo-sub {
		color: rgba(255, 255, 255, 0.5);
		font-size: 12px;
		letter-spacing: 4px;
		margin-top: 4px;
	}

	.hall-nav {
		flex: 1;
		flex-direction: column;
	}

	.hall-nav-item {
		flex-direction: row;
		align-items: center;
		padding: 14px 12px;
		border-radius: 10px;
		margin-bottom: 4px;
	}

	.hall-nav-active {
		background: rgba(212, 175, 55, 0.15);
		border-left-width: 3px;
		border-left-style: solid;
		border-left-color: #d4af37;
	}

	.hall-nav-icon {
		font-size: 18px;
		margin-right: 12px;
	}

	.hall-nav-text {
		color: rgba(255, 255, 255, 0.6);
		font-size: 14px;
	}

	.hall-nav-text-active {
		color: #d4af37;
		font-weight: bold;
	}

	.hall-user {
		flex-direction: row;
		align-items: center;
		background: rgba(255, 255, 255, 0.05);
		border-radius: 12px;
		padding: 12px;
	}

	.hall-user-meta {
		flex-direction: column;
		margin-left: 10px;
	}

	.hall-user-name {
		color: #ffffff;
		font-size: 13px;
		font-weight: bold;
	}

	.hall-user-coin {
		background: rgba(212, 175, 55, 0.2);
		border-radius: 999px;
		padding: 2px 8px;
		margin-top: 4px;
	}

	.hall-user-coin-text {
		color: #d4af37;
		font-size: 11px;
		font-weight: bold;
	}

	/* 右侧主内容 */
	.hall-main {
		flex: 1;
		flex-direction: column;
		padding: 24px;
	}

	.hall-header {
		margin-bottom: 20px;
	}

	.hall-title {
		color: #ffffff;
		font-size: 24px;
		font-weight: bold;
	}

	.hall-subtitle {
		color: rgba(255, 255, 255, 0.5);
		font-size: 13px;
		margin-top: 4px;
	}

	/* 游戏卡片网格 */
	.hall-games {
		flex: 1;
		flex-direction: row;
		flex-wrap: wrap;
	}

	.hall-game {
		width: calc(33.33% - 12px);
		height: 160px;
		margin-bottom: 16px;
		margin-right: 16px;
		border-radius: 14px;
		overflow: hidden;
		border-width: 1px;
		border-style: solid;
		border-color: rgba(212, 175, 55, 0.25);
	}

	.hall-game:nth-child(3n) {
		margin-right: 0;
	}

	.hall-game-bg {
		position: absolute;
		width: 100%;
		height: 100%;
	}

	.hall-game-mask {
		position: absolute;
		width: 100%;
		height: 100%;
		background: linear-gradient(
			180deg,
			rgba(10, 14, 20, 0.2) 0%,
			rgba(10, 14, 20, 0.85) 100%
		);
	}

	.hall-game-content {
		flex: 1;
		flex-direction: column;
		justify-content: space-between;
		padding: 14px;
	}

	.hall-game-name {
		color: #ffffff;
		font-size: 20px;
		font-weight: bold;
	}

	.hall-game-meta {
		flex-direction: row;
		align-items: center;
	}

	.hall-game-meta-text {
		color: rgba(255, 255, 255, 0.6);
		font-size: 12px;
	}

	.hall-game-dot {
		color: rgba(255, 255, 255, 0.3);
		font-size: 12px;
		margin: 0 6px;
	}

	.hall-game-footer {
		flex-direction: row;
		align-items: flex-end;
		justify-content: space-between;
	}

	.hall-game-online {
		flex-direction: row;
		align-items: center;
	}

	.hall-online-dot {
		width: 6px;
		height: 6px;
		border-radius: 3px;
		background: #10b981;
		margin-right: 4px;
	}

	.hall-online-text {
		color: rgba(255, 255, 255, 0.7);
		font-size: 11px;
	}

	.hall-game-btn {
		background: linear-gradient(135deg, #d4af37 0%, #b8941f 100%);
		border-radius: 999px;
		padding: 6px 16px;
	}

	.hall-game-btn-text {
		color: #0a0e14;
		font-size: 12px;
		font-weight: bold;
	}

	.hall-game-pressed {
		opacity: 0.8;
		transform: scale(0.98);
	}

	.hall-pressed {
		opacity: 0.7;
	}
</style>
