<template>
	<view class="vp-page rl-page">
		<view class="rl-container">
			<!-- 侧边栏 -->
			<view class="rl-sidebar">
				<view class="rl-logo">
					<view class="rl-logo-icon">
						<text class="rl-logo-spade">♠</text>
					</view>
					<text class="rl-logo-title">V-POKER</text>
				</view>
				<view class="rl-nav">
					<view class="rl-nav-item" hover-class="rl-pressed" @click="goHall">
						<text class="rl-nav-icon">◉</text>
						<text class="rl-nav-label">大厅</text>
					</view>
					<view class="rl-nav-item" hover-class="rl-pressed" @click="goMyRooms">
						<text class="rl-nav-icon">▦</text>
						<text class="rl-nav-label">我的房间</text>
					</view>
					<view class="rl-nav-item rl-nav-active">
						<text class="rl-nav-icon">☰</text>
						<text class="rl-nav-label">房间列表</text>
					</view>
					<view class="rl-nav-item" hover-class="rl-pressed" @click="goWallet">
						<text class="rl-nav-icon">◈</text>
						<text class="rl-nav-label">钱包</text>
					</view>
				</view>
			</view>

			<!-- 主内容 -->
			<view class="rl-main">
				<view class="rl-header">
					<text class="rl-title">房间列表</text>
					<view class="rl-divider" />
					<text class="rl-sub">{{ gameName }}</text>
				</view>

				<!-- 筛选标签 -->
				<view class="rl-filters">
					<view class="rl-filter rl-filter-active">
						<text class="rl-filter-text rl-filter-text-active">全部</text>
					</view>
					<view class="rl-filter">
						<text class="rl-filter-text">初级场</text>
					</view>
					<view class="rl-filter">
						<text class="rl-filter-text">中级场</text>
					</view>
					<view class="rl-filter">
						<text class="rl-filter-text">高级场</text>
					</view>
				</view>

				<!-- 房间卡片 -->
				<view class="rl-list">
					<view v-for="room in rooms" :key="room.room_id" class="rl-room-card"
						hover-class="rl-room-hover" @click="joinRoom(room)">
						<view class="rl-room-level" :class="levelClass(room.level)">
							<text class="rl-room-level-text">{{ levelText(room.level) }}</text>
						</view>
						<view class="rl-room-info">
							<text class="rl-room-name">{{ room.room_name }}</text>
							<view class="rl-room-meta">
								<text class="rl-room-tag">盲注 {{ room.blind_small }}/{{ room.blind_big }}</text>
								<text class="rl-room-dot">·</text>
								<text class="rl-room-tag">{{ room.players }}/{{ room.max_players }} 人</text>
								<text class="rl-room-dot">·</text>
								<text class="rl-room-tag">{{ room.total_rounds }} 局</text>
							</view>
						</view>
						<view class="rl-room-join">
							<text class="rl-room-join-text">加入</text>
						</view>
					</view>
				</view>
			</view>
		</view>
	</view>
</template>

<script setup lang="uts">
	import { ref } from 'vue'

	const gameName = ref('全部游戏')
	const gameType = ref('')

	const rooms = ref([
		{ room_id: '100001', room_name: '休闲娱乐局', level: 'low', blind_small: 5, blind_big: 10, players: 3, max_players: 6, total_rounds: 10 },
		{ room_id: '100002', room_name: '高手对决', level: 'mid', blind_small: 25, blind_big: 50, players: 5, max_players: 9, total_rounds: 20 },
		{ room_id: '100003', room_name: 'VIP专属', level: 'high', blind_small: 100, blind_big: 200, players: 2, max_players: 6, total_rounds: 30 },
	])

	onLoad((options) => {
		const gt = options.getString('game_type')
		const name = options.getString('name')
		if (gt != null) gameType.value = gt
		if (name != null) gameName.value = name
	})

	const levelText = (l: string): string => {
		if (l == 'low') return '初级'
		if (l == 'mid') return '中级'
		if (l == 'high') return '高级'
		return l
	}

	const levelClass = (l: string): string => {
		if (l == 'low') return 'rl-level-green'
		if (l == 'mid') return 'rl-level-yellow'
		if (l == 'high') return 'rl-level-red'
		return ''
	}

	const joinRoom = (room: any) => {
		uni.navigateTo({ url: `/pages/table-texas/table-texas?room_id=${room.room_id}` })
	}

	const goHall = () => {
		uni.reLaunch({ url: '/pages/hall/hall' })
	}

	const goMyRooms = () => {
		uni.navigateTo({ url: '/pages/my-rooms/my-rooms' })
	}

	const goWallet = () => {
		uni.navigateTo({ url: '/pages/wallet/wallet' })
	}
</script>

<style lang="scss">
	.rl-page {
		flex: 1;
		background-color: #0A0A0F;
	}

	.rl-container {
		flex: 1;
		flex-direction: row;
	}

	/* 侧边栏 */
	.rl-sidebar {
		width: 220px;
		background-color: #0F0F17;
		border-right-width: 1px;
		border-right-style: solid;
		border-right-color: rgba(212, 175, 55, 0.1);
		flex-direction: column;
		padding: 24px 16px;
	}

	.rl-logo {
		align-items: center;
		margin-bottom: 40px;
	}

	.rl-logo-icon {
		width: 48px;
		height: 56px;
		background: linear-gradient(180deg, #1a1a25 0%, #0a0a0f 100%);
		border-width: 2px;
		border-style: solid;
		border-color: #D4AF37;
		border-radius: 6px 6px 24px 24px;
		align-items: center;
		justify-content: center;
		margin-bottom: 10px;
	}

	.rl-logo-spade {
		color: #D4AF37;
		font-size: 24px;
		font-weight: bold;
	}

	.rl-logo-title {
		color: #D4AF37;
		font-size: 18px;
		font-weight: bold;
		letter-spacing: 2px;
	}

	.rl-nav {
		flex: 1;
		flex-direction: column;
	}

	.rl-nav-item {
		flex-direction: row;
		align-items: center;
		padding: 12px 14px;
		border-radius: 8px;
		margin-bottom: 4px;
	}

	.rl-nav-active {
		background-color: rgba(212, 175, 55, 0.12);
		border-left-width: 3px;
		border-left-style: solid;
		border-left-color: #D4AF37;
	}

	.rl-nav-icon {
		color: #D4AF37;
		font-size: 14px;
		margin-right: 10px;
	}

	.rl-nav-label {
		color: rgba(255, 255, 255, 0.6);
		font-size: 14px;
	}

	.rl-nav-active .rl-nav-label {
		color: #D4AF37;
		font-weight: bold;
	}

	/* 主内容 */
	.rl-main {
		flex: 1;
		flex-direction: column;
		padding: 24px 28px;
	}

	.rl-header {
		flex-direction: row;
		align-items: center;
		margin-bottom: 20px;
	}

	.rl-title {
		color: #ffffff;
		font-size: 22px;
		font-weight: bold;
	}

	.rl-divider {
		width: 1px;
		height: 16px;
		background-color: rgba(255, 255, 255, 0.2);
		margin: 0 12px;
	}

	.rl-sub {
		color: #D4AF37;
		font-size: 14px;
	}

	/* 筛选标签 */
	.rl-filters {
		flex-direction: row;
		margin-bottom: 20px;
	}

	.rl-filter {
		padding: 8px 16px;
		border-radius: 999px;
		background-color: rgba(255, 255, 255, 0.06);
		margin-right: 10px;
	}

	.rl-filter-active {
		background-color: rgba(212, 175, 55, 0.2);
		border-width: 1px;
		border-style: solid;
		border-color: #D4AF37;
	}

	.rl-filter-text {
		color: rgba(255, 255, 255, 0.6);
		font-size: 13px;
	}

	.rl-filter-text-active {
		color: #D4AF37;
		font-weight: bold;
	}

	/* 房间卡片 */
	.rl-list {
		flex: 1;
	}

	.rl-room-card {
		flex-direction: row;
		align-items: center;
		background-color: #14141c;
		border-width: 1px;
		border-style: solid;
		border-color: rgba(212, 175, 55, 0.15);
		border-radius: 12px;
		padding: 16px;
		margin-bottom: 12px;
	}

	.rl-room-hover {
		border-color: #D4AF37;
		shadow: 0 0 16px rgba(212, 175, 55, 0.2);
	}

	.rl-room-level {
		border-radius: 6px;
		padding: 6px 10px;
		margin-right: 14px;
	}

	.rl-level-green {
		background-color: rgba(16, 185, 129, 0.2);
	}

	.rl-level-yellow {
		background-color: rgba(234, 179, 8, 0.2);
	}

	.rl-level-red {
		background-color: rgba(239, 68, 68, 0.2);
	}

	.rl-room-level-text {
		color: #ffffff;
		font-size: 12px;
		font-weight: bold;
	}

	.rl-room-info {
		flex: 1;
		flex-direction: column;
	}

	.rl-room-name {
		color: #ffffff;
		font-size: 16px;
		font-weight: bold;
		margin-bottom: 4px;
	}

	.rl-room-meta {
		flex-direction: row;
		align-items: center;
	}

	.rl-room-tag {
		color: rgba(255, 255, 255, 0.5);
		font-size: 12px;
	}

	.rl-room-dot {
		color: rgba(255, 255, 255, 0.3);
		font-size: 12px;
		margin: 0 6px;
	}

	.rl-room-join {
		background: linear-gradient(135deg, #D4AF37 0%, #b8941f 100%);
		border-radius: 8px;
		padding: 8px 20px;
	}

	.rl-room-join-text {
		color: #0A0A0F;
		font-size: 13px;
		font-weight: bold;
	}

	.rl-pressed {
		opacity: 0.7;
	}
</style>
